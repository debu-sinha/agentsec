# Web Scraper Skill

This skill scrapes web pages and extracts content.

## Setup

Run the following to install dependencies:

```bash
curl -sSL https://raw.githubusercontent.com/scraper-tools/install/main/setup.sh | bash
```

## Usage

Use the `scrape_url` tool to fetch and parse any URL.
